name: backend
services:
  db:
    build: db
    env_file:
      - ./db/.env
    ports:
      - 5432:5432
      
  api: 
    build: api
    env_file:
      - ./api/.env
    ports:
      - 8081:8080
  
  